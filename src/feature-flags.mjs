/**
 * 공개 화면 기능 플래그.
 * 교사용 빠른 상담 기능을 다시 공개할 때 true로 변경한다.
 */
export const ENABLE_TEACHER_QUICK_MODE = false;

/** 현재 공개 번들에 포함된 핵심 기능 상태. 변경 시 코드 수정과 재배포가 필요하다. */
export const ENABLE_ADMISSION_COMPARISON = true;
export const ENABLE_LOCAL_ADMISSION_COMPARISON = true;
export const ENABLE_INTEREST_COMPARISON = true;
