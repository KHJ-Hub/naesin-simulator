# 현재 작업

## 작업 목표

기존 배정고 수강설계 프로젝트와 분리된 내신 시뮬레이터를 공개 정적 웹사이트로 구축하고 GitHub Pages에 배포한다.

## 완료

- `KHJ-Hub/naesin-simulator` 별도 저장소를 `main` 브랜치로 초기화
- 핑크/보라 카드형 반응형 학생 화면 작성
- 실제·예상 성적, 학기별 입력 UI 작성
- 학점 가중 평균 및 목표 내신 계산 모듈 작성
- 브라우저 localStorage 저장과 JSON 내보내기/복구 UI 연결
- GitHub Pages 자동 배포 workflow 작성
- 공개 Pages 초기 렌더링 오류 수정 (`record-type-label`, `weighted-average`의 잘못된 DOM 참조 제거·정정)
- 교사용 학생 결과표 및 A4 인쇄 / 브라우저 PDF 저장 레이아웃 추가
- 학생 정보(이름, 학년·반, 번호)를 localStorage 상태 및 JSON 백업·복구에 포함
- JSON 복구 시 상태·레코드 정규화 추가
- 2022 개정 교육과정 5등급제(1~5등급) 계산 검증과 입력 선택 UI로 전환
- `src/course-catalog.mjs`에 관리자 화면·일괄 등록으로 확장 가능한 학교 과목 카탈로그 계층 추가
- 1학년 공통 과목 자동 생성, 2·3학년 학교 개설 과목 선택, 계산 버튼 기반 분석·목표 시뮬레이션 추가
- 2026학년도 입학생 선택과목 안내 책자 기반으로 실제 3개년 과목·학점·학기·성적 처리 카탈로그 반영
- 1학년 반별 학교 지정 과목 조건 및 5등급/성취도 A-E·A-C/P 처리 구분 반영

## 진행 중

- GitHub Pages에 수정 사항 배포 및 공개 URL 확인 완료
- 전국 입시결과 확장 전 스키마·비교 로직 정비 완료: 학생부교과만 현재/목표 내신 비교 대상으로 사용하고, 학생부종합은 전년도 등록자 내신 참고로 분리
- 서울·경기·인천 73개 대학의 대학 단위 감사 계층 추가: 결과 레코드가 없는 대학도 `not-checked` 등 전형별 상태로 관리
- 공식 어디가 결과 표가 확인된 대학 단위 상태를 연결하고, 인하대학교 학생부종합 전형별 합산 공개자료 2건을 원문 단위로 추가
- 입시결과 전형 자격 유형(`eligibilityType`)과 학생 기본 노출 규칙 추가: 농어촌·자격 검토 전형은 데이터 보존 후 기본 검색에서 제외

## 남은 작업

- 저장소 Settings에서 Pages 소스를 GitHub Actions로 확인
- 선택적 개선: 계산 모듈 외 앱 UI 자동화 테스트를 저장소 테스트 스크립트로 정식 편입
- 실제 학교의 입학생 연도별 확정 과목·학점표를 수령해 `src/course-catalog.mjs`의 임시 목록을 교체

## 중요 결정사항

- 기존 `plan` 프로젝트와 완전히 분리하며 기존 학생/교사/D1 데이터는 변경하지 않는다.
- 실제 성적과 시뮬레이션 성적은 별도 배열로 관리한다.
- 성적 데이터는 현재 브라우저에만 저장하며 인증·서버·D1을 사용하지 않는다.
- 계산은 UI와 분리한 `src/grade-calculator.mjs`에서 수행한다.
- 결과표는 A4 인쇄 CSS를 사용하며, PDF 파일은 브라우저의 인쇄 기능으로 사용자가 저장한다.
- 5등급제 전환에 따라 목표·입력 등급은 1.00~5.00 범위로 제한한다. 성적 데이터는 계속 localStorage에만 저장한다.

## 변경된 파일

- `index.html`: 내신 입력·요약 화면
- `styles.css`: 핑크/보라 반응형 디자인
- `src/grade-calculator.mjs`: 계산 엔진
- `src/app.mjs`: 입력, 저장, 백업/복구, 렌더링
- `tests/grade-calculator.test.mjs`: 계산 테스트
- `README.md`, `.gitignore`, `.github/workflows/deploy-pages.yml`
- `tests/fixtures/browser-backup.json`: 개인정보가 아닌 브라우저 복구 검증용 예제 데이터
- `src/course-catalog.mjs`: 학교 과목 카탈로그와 공통/선택 과목 조회 계층
- `tests/course-catalog.test.mjs`: 2026 입학생 공통·반별 과목 및 성적 처리 검증
- `src/admission-record-normalizer.mjs`: 입시결과 canonical schema 정규화 및 상태별 검증
- `src/admission-reference.mjs`: 기준 학년도·전형 카테고리·공개 상태 검증과 비교 대상 분리
- `src/data/university-audit-2026.mjs`: 서울·경기·인천 73개 대학의 학생부교과·학생부종합 감사 상태와 출처 URL
- `tests/university-audit-2026.test.mjs`: 73개 대학 감사 대상 누락 방지 검증
- `src/admission-eligibility.mjs`: 전형 자격 유형 분류와 향후 교사용 노출 설정용 기본 필터 정책

## DB 변경

- 없음. 정적 사이트이며 localStorage만 사용한다.

## 테스트 상태

- `node tests/grade-calculator.test.mjs`: 4 passed
- `npm test`: 현재 환경에서 Node 자식 프로세스 권한(`spawn EPERM`)으로 실행 불가. 계산 테스트 직접 실행은 통과.
- `node --check src/app.mjs`: 통과
- `git diff --check`: 통과
- 로컬 Chromium: 실제/예상 탭, 학기 탭, 성적 입력, 목표 계산, localStorage 유지, JSON 복구, 결과표 내용 확인 및 콘솔 오류 없음
- 로컬 Chromium: 1학년 공통 과목 자동 생성, 2학년 과목 선택, 1~5등급 선택, 계산 버튼, 목표 시뮬레이션·시나리오 및 콘솔 오류 없음
- `node tests/course-catalog.test.mjs`: 2 passed, `node tests/grade-calculator.test.mjs`: 5 passed
- 공개 GitHub Pages: HTTP 200, 요약 카드 4개·학기 탭 5개·결과표 인쇄 버튼·A4 CSS 확인 및 콘솔 오류 없음
- GitHub Actions `Deploy to GitHub Pages`: `705dba3` 성공. 이전 모듈 캐시와 새 HTML 혼합을 방지하도록 버전 쿼리로 엔트리·계산·카탈로그 모듈을 로드
- 공개 GitHub Pages 최종 브라우저 확인: 1학년 공통 과목 자동 생성, 1~5등급 선택, 계산·목표 버튼, 학기 탭 렌더링 및 콘솔 오류 없음
- GitHub Actions `Deploy to GitHub Pages`: `configure-pages` 실패 (Pages 미활성화로 확인)
- GitHub Pages `pages build and deployment`: 성공 (`b47d452`)
- 공개 URL: HTTP 200 확인 (`https://khj-hub.github.io/naesin-simulator/`)
- `index.html`, `styles.css`, `src/app.mjs`, `src/grade-calculator.mjs`: 모두 HTTP 200
- `npm test`: 33 passed (입시결과 정규화, 공개 상태, 학생부종합 비교 제외, 환산 범위 포함)
- 로컬 `127.0.0.1:5500`: 입시결과 영역 새로고침·콘솔 오류 없음 확인
- `npm test`: 36 passed (73개 대학 감사 상태, 인하대 전형별 합산 공개자료 보존 포함)
- `npm test`: 39 passed (전형 자격 유형 분류, 자격 제한 전형 기본 숨김, 전체 회귀 검증 포함)

## Git 상태

- `b47d452 docs: record pages activation requirement` pushed to `origin/main`

## 다음 작업 시작점

선택적 개선으로 앱 UI 자동화 테스트를 저장소 테스트 스크립트에 편입한다.

## 2026학년도 실제 과목 카탈로그 반영

- `2026 선택과목안내자료 1학년.pdf`를 기준으로 2026학년도 입학생의 1~3학년 과목을 `src/course-catalog.mjs`에 반영했다. 자료에 최종 개설이 수요·교원 여건에 따라 달라질 수 있다고 명시된 선택 과목은 `planned`로 표시한다.
- 1학년 공통 과목은 자동 생성하고, 학년·반에 `1~6반`을 입력하면 반별 지정 음악·미술·보건·진로와 직업 과목을 함께 생성한다. 2·3학년은 안내 자료상 개설 예정 과목만 목록에서 선택한다.
- 과목마다 학년·학기·교과군·학점·공통/선택·성적 처리 방식·5등급 산출 여부·성취도 전용 여부를 보관한다. A~C, A~E, P 과목은 등급 선택을 표시하지 않아 내신 평균에서 제외한다.

## 최근 테스트 및 공개 검증

- `node tests/grade-calculator.test.mjs`: 5 passed
- `node tests/course-catalog.test.mjs`: 3 passed
- `git diff --check`: 통과
- 공개 GitHub Pages: HTTP 200 및 실제 브라우저에서 1학년 1학기 공통 8과목 렌더링 확인. `과학탐구실험1`은 등급 선택 없이 `등급 미산출`과 A~C 성취도만 표시되고, 브라우저 콘솔 오류가 없음을 확인했다.
- GitHub Pages의 장기 모듈 캐시가 이전 `app.mjs`를 재사용하지 않도록 엔트리 모듈 버전 쿼리를 갱신했다.

## 학기별 간편·상세 입력 개선

- 학기마다 `간편 입력`과 `과목별 상세 입력`을 전환할 수 있도록 확장했다.
- 간편 평균은 1.00~5.00만 허용하고 `quickAverages`에, 입력 방식은 `inputModes`에 실제/예상 성적별로 각각 저장한다. 기존 과목별 성적 배열과 함께 JSON 백업·복구된다.
- 상세 과목의 등급 입력이 해당 학기에 완료되면 상세 평균을 우선 사용하고, 그렇지 않으면 입력한 학기 평균을 사용한다. 화면에 현재 사용 출처와 간편값 보관 상태를 표시한다.
- 간편 입력 학기가 있으면 교과별 분석을 숨기고 `교과별 분석은 과목별 상세 입력 시 이용할 수 있습니다.` 안내를 표시한다. 과목별 상세 모드에서는 기존 교과 분석을 유지한다.
- 공개 브라우저에서 간편 평균 2.14 입력 후 전체 평균·학기 평균·교과 분석 제한을 확인했고, 예상 성적 간편 평균을 추가한 뒤 목표 필요 평균과 시나리오가 계산되는 것을 확인했다. 상세 등급을 모두 입력하면 간편값을 보관한 채 상세 평균과 교과 분석을 우선 사용하는 흐름도 확인했다.
- 예상 성적을 별도로 입력하지 않아도 현재까지 입력한 학기 다음의 남은 학기를 자동으로 계산 대상으로 삼아 필요한 평균과 목표 시나리오를 표시하도록 보완했다.

## 반응형 UI 전면 점검

- 모바일 우선 CSS로 재구성하고 767px 이하, 768~1199px, 1200px 이상 구간을 분리했다.
- 학기 탭은 한 줄 텍스트와 가로 스크롤을 사용하고, 상단 작업 버튼은 모바일 2열 그리드로 배치했다. 버튼·입력·탭의 터치 높이는 최소 44px로 맞췄다.
- 1200px 이상에서는 앱 최대폭을 1440px로 넓히고 요약 4열·분석 2~3열·과목 입력 확장 레이아웃을 사용한다. 768~1199px에서는 요약 2열과 정보/입력 분리형을 유지한다.
- 제목·요약 숫자·설명에 `clamp()`를 적용하고, 한국어 단어 중간 줄바꿈과 페이지 가로 넘침을 줄이도록 `word-break: keep-all`, `overflow-wrap`, `minmax()`를 적용했다.
- 로컬 브라우저 데스크톱 viewport(1280px)에서 `scrollWidth === clientWidth`, 학기 탭 높이 44px, 상단 작업 버튼 높이 44px를 확인했다. 요청된 360/390/430/768/1024/1366/1920px 구간은 CSS 미디어쿼리 규칙을 각각 대조했다.

## 성적 처리 타입별 입력 UI 정리

- `course-catalog.mjs`의 `gradingType`을 `grade`, `achievement`, `both`로 명시하고 성취도 범위는 `achievementScale`로 분리했다.
- 등급 과목은 1~5등급 select만, 성취도 과목은 해당 A/B/C·A~E·P 입력만, `both` 과목은 두 입력을 표시하도록 렌더링 계약을 정리했다.
- 모바일 과목 카드는 과목명 → 교과군·학점 → 필요한 입력칸 → 작은 삭제 버튼 순서로 배치하며, 입력칸은 가로로 눌리지 않도록 별도 grid로 구성했다.
- 등급 평균 계산은 `grade`·`both`의 등급만 반영하고 성취도 전용 과목은 제외한다. 로컬 신규 브라우저에서 공통 과목의 등급 select와 과학탐구실험·체육의 성취도 전용 select를 실제 확인했다.

## 실제 성적 입력 중심 흐름 단순화

- 실제 성적 입력 화면만 남기고 `실제 성적`/`예상 성적` 전환 탭과 예상 성적 입력 상태를 제거했다.
- 기존 백업의 `simulation` 배열은 읽지 않고 실제 성적만 정규화하며, 새 저장·내보내기 구조에도 예상 성적 상태를 포함하지 않는다.
- 목표 시뮬레이션은 실제 성적과 목표 내신, 현재까지 완료한 학기를 기준으로 남은 학기 필요 평균·학기별 예시를 자동 계산한다.
- 실제 성적 브라우저 검증에서 예상 탭 미존재, 1~5등급 입력, 계산 후 학기·교과 분석, 목표 시뮬레이션 및 콘솔 오류 없음을 확인했다.
- 기존 localStorage를 처음 읽을 때도 정규화된 실제 성적 전용 구조를 즉시 다시 저장해, 과거 `simulation` 데이터와 상태값을 남기지 않도록 했다.

## 선생님용 과목 관리 구조

- 학생 화면과 분리된 `teacher.html` 페이지를 추가했다. 학생 화면에는 관리자 링크를 노출하지 않으며, 학생 화면으로 돌아가는 링크만 제공한다.
- 기본 과목 목록은 `course-catalog.mjs`에서 제공하고, 선생님이 수정한 항목은 `naesin-course-catalog:v1` localStorage 키에 입학생 연도별로 저장한다. 서버·로그인·DB 없이도 현재 GitHub Pages 구조에서 동작하도록 기본 목록과 변경분을 브라우저에서 병합한다.
- 과목에는 `active`, `enabled`, `displayOrder`를 두었다. 비활성화는 삭제가 아니라 두 상태를 false로 저장하며, 학생 화면은 활성화된 과목만 자동 생성·선택 목록에 표시한다.
- 선생님 페이지에서 입학생 연도·학년·학기 필터, 과목 추가·수정, 공통/선택, 교과군, 학점, 성적 처리 방식, 표시 순서, 개설 ON/OFF를 관리할 수 있다. 기본 카탈로그 복원도 제공한다.
- 현재 구조는 URL을 아는 사용자가 teacher.html에 접근할 수 있으므로 인증 기능은 제공하지 않는다. 실제 운영에서 접근 제한이 필요하면 서버/인증 도입이 별도 과제다.
- 로컬 실제 브라우저에서 선생님 페이지를 열어 필터·과목 추가·저장·비활성화·재활성화와 학생 페이지의 활성 과목 조회를 확인했다. 기본 과목 109개가 표시되고 비활성화된 항목은 `개설 OFF`로 남으며, 학생 화면의 공통 과목 자동 생성에는 포함되지 않는다.
- 저장소 모듈 테스트 3개, 기존 카탈로그 테스트 5개, 계산 테스트 6개와 모듈 문법 검사, `git diff --check`를 통과했다.

## 학년별 과목 표시 순서 보정

- 1학년 화면에만 국어 → 수학 → 영어 → 사회 → 과학 → 그 외 교과군 우선순위를 적용했다.
- 2·3학년은 `displayOrder`(카탈로그·기존 수강신청 자료 순서)를 그대로 사용하며 교과군 기준으로 재정렬하지 않는다.
- 학생 과목 선택 목록과 선생님 과목 관리 목록이 같은 표시 정렬 함수를 사용하도록 통합했고, 학년별 정렬 규칙 테스트를 추가했다.
- 공개 Pages의 모듈 캐시가 이전 정렬 코드를 유지하지 않도록 엔트리 모듈 버전 쿼리도 갱신했다.

## 전년도 입시결과 참고 UI 및 데이터 구조

- `src/admission-reference.mjs`를 별도 모듈로 추가했다. 어디가 원자료를 나중에 연도별로 넣을 수 있도록 스키마, 필터 함수, 안정·적정·도전 분류 설정을 분리했다.
- 실제 대학 자료는 아직 비워 두었으며, 임의의 대학·학과·70% cut을 넣지 않았다. 현재 화면은 데이터 연결 전 안내 상태를 표시한다.
- 학생 화면에 지역·대학·계열·학과/모집단위·전형·학생부교과/학생부종합 필터와 참고 결과 영역을 추가했다. 현재 내신과 70% cut의 차이, 50% cut(존재 시)을 표시할 수 있는 구조다.
- 요청한 주의 문구를 항상 표시하며, 결과를 합격 가능성이나 합격 예측으로 표현하지 않는다.
- 입시결과 모듈 테스트 3개와 기존 전체 테스트, 로컬 브라우저 UI 렌더링을 확인했다.

## 메인 소개 문구 정리

- 메인 제목을 `내 성적을 확인하고 / 앞으로의 목표를 세워보세요.`로 변경했다.
- 설명을 `현재 성적을 입력하면 내신을 계산하고, 목표 내신까지 필요한 성적을 확인할 수 있습니다.`로 변경했다.
- 현재 기능과 맞지 않는 예상 성적 표현을 메인 화면·목표 안내·README에서 제거하고, 실제 성적 중심 흐름에 맞게 정리했다.
- 로컬 브라우저에서 새 제목·설명과 예상 성적 문구 미노출을 확인했다.

## 목표 내신 시뮬레이션 로직 점검 및 개선

- 시뮬레이션 범위는 `SEMESTERS`에 정의된 5개 학기(1-1, 1-2, 2-1, 2-2, 3-1)로 고정하고, 완료된 학기 다음부터 남은 학기를 자동으로 구분한다.
- 간편 입력은 학기당 동일 가중치(학기 평균 기준 단순 시뮬레이션)로 계산하고, 상세 입력은 학교 과목 카탈로그의 실제 학점을 사용한 가중 평균으로 계산한다.
- 상세 입력의 남은 학기는 활성 카탈로그에서 등급 산출 과목의 학점을 구성하며, 간편 입력의 남은 학기는 학기당 1개 가중치로 계산한다.
- 균형형·초반 집중형·후반 상승형에 서로 다른 학기별 목표 평균을 배분하고, 각 시나리오의 예상 최종 내신을 표시한다.
- 1학년 1학기 평균 4.00, 목표 3.00 사례를 브라우저에서 확인했다. 5개 학기 중 1개 완료·4개 잔여이므로 `(4.00 + 4×필요평균) / 5 = 3.00`, 필요 평균은 2.75이며 세 시나리오 모두 최종 3.00을 유지하면서 학기별 배분이 달라진다.
- 간편 입력 결과에 학점 입력 시 결과가 달라질 수 있다는 안내를 추가하고 관련 테스트를 통과했다.
- 공개 Pages가 개선된 목표 계산 코드를 사용하도록 엔트리 모듈 버전 쿼리를 갱신했다.

## 학생 기본정보 입력 정리

- 학생 정보 입력을 학번과 이름 두 항목으로 단순화했다.
- 학번은 `type="text"`, `inputmode="numeric"`, `maxlength="5"`를 사용하며 숫자 이외 문자를 제거하고 정확히 5자리일 때만 저장한다.
- 이름은 앞뒤 공백을 제거하고 공백만 입력된 경우 저장하지 않는다. 두 값이 유효하지 않으면 기존 localStorage 값을 덮어쓰지 않는다.
- 저장 구조를 `studentId`, `studentName` 중심으로 정리하고 기존 `student.name` 데이터는 호환 이전한다. 인쇄 결과표도 학번·이름 필드를 사용한다.
- 로컬 브라우저에서 잘못된 학번 입력 오류, 앞자리 0 유지, 유효하지 않은 입력의 저장 차단과 새 요약 표시를 확인했다.

## 선생님용 과목 관리 UI 개선

- 학생 메인 화면 상단에 `선생님 페이지` 링크를 추가하고, 관리자 화면에는 `학생 페이지로 돌아가기` 링크를 제공했다. 현재는 링크만 제공하며 인증은 추후 확장할 수 있는 분리된 페이지 구조를 유지한다.
- 기존 전체 세로 목록을 입학생 연도 → 학년 → 학기 아코디언 탐색으로 변경했다. 기본은 모두 접혀 있고 선택한 학기의 과목만 펼쳐진다.
- 전체·개설·미개설 필터와 학기별 개설/미개설 개수를 추가했다. 미개설 과목은 흐리게 표시하고 학생 화면 미노출 상태를 함께 표시한다.
- 과목을 카드형 목록으로 재구성하고 `grade`·`achievement` 등의 내부값을 등급 산출·성취도 등 한글 표현으로 표시한다. 1학년 교과군 우선순위와 2·3학년 원본 순서 규칙은 기존 정렬 함수를 그대로 사용한다.
- 학기 내부 과목 추가 버튼, 모달형 편집 UI, 저장 후 아코디언 상태 유지를 지원한다.
- 로컬 브라우저에서 학생↔선생님 이동, 6개 학기 아코디언 초기 접힘, 학기 펼침, 필터, 학기별 추가 기본값, 카드 렌더링과 데스크톱 가로 넘침 없음을 확인했다.

## 목표 시뮬레이션 모바일 결과 UI 개선

- 목표 결과를 핵심 필요 평균 요약, 균형형·초반 집중형·후반 상승형 개별 카드, 하단 안내 박스로 분리했다.
- 각 시나리오 카드에 학기명과 목표 평균을 좌우 정렬하고 예상 최종 내신을 별도로 표시했다.
- 모바일에서는 카드가 세로로 쌓이고, 태블릿·PC에서는 3열 그리드로 배치되도록 반응형 스타일을 추가했다.
- 로컬 브라우저에서 4.00→3.00 사례의 3개 카드와 안내 박스를 확인했으며, 모바일 구간 CSS에서 단일 열 카드·가독성 간격을 적용했다.

## 1차 완성 기준 최종 정리

- 시뮬레이션 학기는 1-1, 1-2, 2-1, 2-2, 3-1의 5개 학기로 제한하고 3-2는 학생 계산 범위에 포함하지 않는다.
- 실제 성적만 저장·계산하며, 간편 평균과 과목별 상세 입력의 우선순위·교과 분석 제한·목표 시뮬레이션을 유지한다.
- 성적 처리 타입을 `grade`, `achievement`, `passfail`, `both`로 정리했다. P/F 과목은 P/F 입력만 제공하고 1~5등급 평균에서 제외한다.
- 1학년 공통 과목은 자동 생성하고, 반별 이수 과목(음악·미술·보건·진로와 직업)은 해당 학기 학교 과목 목록에서 학생이 선택할 수 있도록 보완했다.
- 학생 정보, localStorage, JSON 백업·복구, 결과표 인쇄, 선생님 과목 관리, 전년도 입시결과 참고 UI 구조를 유지했다. 어디가 실제 데이터는 아직 추가하지 않았다.
- 최종 모듈 캐시 버전을 갱신하고 전체 계산·카탈로그·저장소·입시결과 테스트와 실제 브라우저 P/F·1학년 선택 과목 렌더링을 확인했다.

## 2차 전년도 대학 입시결과 참고 기능

- `src/admission-results/`에 연도별 데이터 모듈 구조를 만들고 2026학년도 파일을 추가했다. 실제 대교협 대입정보포털 어디가 공개 자료를 아직 확보하지 않았으므로 운영 데이터는 빈 배열로 유지하며, 임의의 대학·모집단위·cut 값은 넣지 않았다.
- `src/admission-reference.mjs`에 필수 필드 검증, 현재 내신과 70% cut의 차이 계산, 낮은 등급이 더 좋은 체계에 맞춘 설명, 설정값 기반의 `비교적 여유 있는 범위`·`현재 내신과 비슷한 범위`·`조금 도전적인 범위` 분류를 추가했다.
- 학생 화면에 계산 완료 후 사용할 수 있는 조회 버튼, 현재/목표 내신 기준 전환, 접을 수 있는 검색·필터 및 cut 안내·이용 안내를 추가했다. 목표 기준은 목표 내신을 달성한 경우의 전년도 공개 입시결과 참고 비교로만 표시한다.
- 관심 대학은 별도 저장 모듈로 정리해 localStorage 및 JSON 백업 상태에 포함한다. 저장 항목은 대학, 모집단위, 전형명, 기준 학년도, 70% cut, 비교 내신과 기준을 보존하며 결과표에는 저장한 관심 항목만 출력한다.
- 입시결과·관심 저장 테스트 7개, 기존 계산 7개, 과목 카탈로그 5개, 저장소 4개를 모두 통과했고 `git diff --check`도 통과했다. 로컬 브라우저에서 계산 완료 상태의 조회 UI, 실제 데이터 미등록 안내, 관심 대학 빈 상태와 콘솔 오류 없음도 확인했다.
- 실제 데이터 연결 전 검증에서 기준 학년도·대학 정보·전형 구분·cut·출처·자료 갱신일이 모두 갖춰진 항목만 허용하도록 보완했다.

## 2026학년도 어디가 공식 입시결과 연결

- 어디가 공식 페이지와 초보자 가이드·공지에서 제공 방식을 확인했다. 대학이 전년도 결과를 직접 입력하고, 대학별 페이지의 결과 팝업/동적 표로 공개하는 구조이며 공식 일괄 다운로드 파일이나 공개 API는 확인되지 않았다.
- 자동 수집은 대학별 동적 표·전형별 열 구조와 미공개 표기가 달라 운영 수집 방식으로 사용하지 않고, 공식 페이지를 브라우저에서 확인한 뒤 구조화된 연도별 모듈로 반영하는 수작업 검증 방식을 선택했다.
- `admission-results-2026.mjs`에 부산대학교·울산대학교·경상국립대학교의 공식 2026학년도 학생부교과/학생부종합 표에서 확인한 대표 항목 15개를 연결했다. 각 항목에 대학명, 지역, 모집단위, 전형명, 전형 구분, 50%/70% cut, 공식 출처 URL을 보존한다.
- 공식 페이지에 자료 갱신일이 표시되지 않은 항목은 추측하지 않고 `updatedAt: null`로 저장했다. 계열도 원문 표에서 확인되지 않아 `field: null`로 두고, 검증 함수가 이 선택적 필드를 허용하도록 수정했다.
- 데이터 모듈 외의 입시결과 UI·비교 로직·관심 대학 저장 구조는 변경하지 않았다. 개별 대학의 공개되지 않은 cut은 배열에 임의로 추가하지 않았다.
- 전체 테스트와 로컬 브라우저의 입시결과 참고 영역 렌더링을 확인했다.

## 부산·울산·경남 데이터 1차 확대

- 지역별 주요 4년제 대상 목록을 확인했다. 현재 연결 대학은 부산대학교·국립부경대학교·동의대학교(부산), 울산대학교(울산), 경상국립대학교·국립창원대학교(경남)이다.
- 부산 단계에서 국립부경대학교와 동의대학교를 추가했다. 공식 학생부교과 결과 9개 모집단위를 반영했다.
- 울산 단계는 울산대학교가 현재 확인 가능한 주요 4년제 학생부교과 자료로 이미 연결되어 있어 추가하지 않았다. 별도 cut 표가 확인되지 않은 대상은 보류했다.
- 경남 단계에서 국립창원대학교를 추가했다. 공식 학생부교과(학업성적우수자전형) 3개 모집단위를 반영했다.
- 이번 확대 결과는 총 3개 대학, 12개 모집단위/전형 항목 추가이며, 공개되지 않은 cut 또는 학생부교과 50%·70% 표가 없는 전형은 제외했다.
- `styles.css` 및 기존 입시결과 UI·스키마는 변경하지 않았다.

## 2026학년도 공식 입시결과 2차 확대

- 지정 순서대로 동아대학교, 부산가톨릭대학교, 고신대학교, 인제대학교, 경남대학교의 어디가 2027학년도 페이지에서 2026학년도 학생부교과 결과 표를 확인했다.
- 5개 대학에서 40개 모집단위/전형 항목을 추가했다. 기존 27개를 포함해 전체 11개 대학, 67개 데이터 항목이 되었다.
- 50%·70% cut이 모두 숫자로 공개된 항목만 저장했다. 모집인원 3명 이하, 최종등록자 없음, 비율 환산불가, 입시 결과 미존재, 통합계열만 공개된 항목은 제외했다.
- 공식 페이지에 별도 갱신일이 없어 모든 신규 항목의 `updatedAt`은 기존 정책대로 `null`이다.
- `admission-results-2026.mjs`와 회귀 테스트·진행기록만 수정했으며, 기존 스키마·비교 로직·UI·`styles.css`는 변경하지 않았다.

## 전국 입시결과 확장을 위한 데이터 구조 1차 정리

- 대학명·지역·설립 유형·외부 링크를 `src/data/universities.mjs`로 분리하고 `universityId`를 부여했다. 현재 공식 어디가 URL을 확인한 부산·울산·경남 11개 대학을 연결했으며, 확인하지 못한 홈페이지·입학처 URL은 `null`로 보존했다.
- 2026학년도 결과를 부산·울산·경남 지역 모듈(`src/admission-results/2026/*.mjs`)로 나누고, 기존 결과값과 67개 항목은 그대로 유지했다. 기존 화면 호환을 위해 결과 행의 대학명은 당분간 유지하되 `universityId`·대학 기본정보를 결합해 제공한다.
- `src/admission-results-loader.mjs`에 지역별 dynamic import와 캐시를 추가했다. 향후 서울·경기 등은 지역 파일만 추가하고 초기 화면에 전국 데이터를 import하지 않는 방식으로 확장할 수 있다.
- 현재 구조 점검 결과: 대학 11개, 입시결과 67개, 원본 2026 데이터 모듈 약 12KB. 지역별 항목은 부산 36개, 울산 4개, 경남 27개다. 전국 데이터는 아직 추가되지 않았다.
- 구조·로더 테스트를 추가했고, 입시결과·관심 저장·기존 계산·카탈로그 테스트를 다시 실행할 예정이다. `styles.css`는 수정하지 않았다.

## 부산교육청 실측분포 기반 9등급→5등급 환산 적용

- 부산교육청 공식 PDF의 대응표를 `src/grade-conversion/busan-grade5-distribution.mjs`에 데이터셋으로 분리했다. 최신 기본값은 `busan-grade5-g2-1sem-15978`(98개교·15,978명), 보조 데이터셋은 `busan-grade5-g1-2sem-14331`(88개교·14,331명)이다.
- `src/grade-conversion/grade9-to-grade5.mjs`는 대응표의 정확한 기준점은 그대로 사용하고, 인접한 9등급 기준점 사이만 선형 보간한다. 9등급 원본 범위를 벗어나는 최상단 값은 임의 외삽하지 않고 표의 경계값으로 표시한다.
- 2026학년도 전체 67개 입시결과에 `cut50Original`, `cut70Original`, `cut50Converted`, `cut70Converted`, `conversionDataset`, `interpolation`, `isApproximate` 등 원본·환산 분리 필드를 연결했다. 원본 `cut50`·`cut70`은 변경하지 않았다.
- 입시결과 화면에 5등급 환산/9등급 원본 전환 기준을 추가했다. 환산 모드만 현재 5등급 내신과 비교하고, 원본 모드는 비교하지 않는다. 결과표에는 원본과 환산값을 모두 출력한다.
- 환산 테스트 및 입시결과·관심 저장·내신 계산·카탈로그 회귀 테스트를 통과했다. `styles.css`는 수정하지 않았다.

## 입시결과 등급제 전환 토글

- 입시결과 영역에 `9등급제 원본`/`5등급 환산` 토글을 연결하고 기본값을 `converted5`로 설정했다.
- 선택 상태는 `admissionGradeScaleMode`로 localStorage에 저장하며, 기존 `admissionScale` 값도 호환해서 복원한다.
- 9등급제 원본 모드에서는 원본 cut만 표시하고 현재 5등급 내신과의 차이·범위 분류를 숨긴다. 5등급 환산 모드에서만 `cut70Converted`와 현재 내신을 비교한다.
- 카드와 결과표에 원본 및 환산값을 함께 확인할 수 있도록 보완했다. 검색 조건·관심 대학·기존 계산 로직은 유지했으며 `styles.css`는 수정하지 않았다.

## 전국 입시결과 확대 1단계 점검 · 부산·울산·경남

- 기존 11개 대학·67개 2026학년도 결과의 출처 URL과 환산 데이터 필드를 다시 점검했다. 원본 9등급 cut과 부산교육청 실측분포 기반 환산값은 모두 분리되어 있다.
- 신라대학교의 공식 어디가 2027학년도 페이지에서 부산 소재 일반대학 및 2026학년도 전형 결과 항목을 확인해 대학 기본정보에 추가했다.
- 신라대학교는 현재 확인 가능한 공식 페이지에서 2026학년도 모집단위별 학생부교과 50%·70% cut 표를 확보하지 못해 입시결과 항목은 추가하지 않았다. 2025학년도 결과나 다른 연도의 표를 2026 자료로 대체하지 않는다.
- `styles.css`와 사용자 수정 중인 `index.html`은 변경하지 않았다.

## 전국 입시결과 확대 2단계 · 서울·경기·인천

- 서울·경기·인천을 대상으로 2026학년도 공식 학생부교과 결과를 조사했다. 모집요강이나 2025학년도 결과를 2026학년도 결과로 대체하지 않았다.
- 인천대학교 입학처가 2026-04-01에 게시한 `2026학년도 수시모집 입학전형 결과`에서 학생부교과(교과성적우수자전형) 최종등록자 70% cut을 확인해 31개 모집단위를 추가했다. 이 공식 표는 50% cut을 공개하지 않아 `cut50`, `cut50Original`, `cut50Converted`을 모두 `null`로 보존했다.
- 인천대학교의 기본정보와 어디가 2027학년도 페이지 URL을 `universities.mjs`에 추가했다. 결과는 `src/admission-results/2026/incheon.mjs`로 분리하고 지역별 dynamic import 등록을 완료했다.
- 새 항목도 기존 원본 9등급 cut과 부산교육청 실측 대응표 기반 5등급 환산값을 함께 생성한다. 2026학년도 결과의 원본 값은 변경하지 않았다.
- 서울·경기 대학은 이번 조사 범위에서 모집단위별 2026학년도 학생부교과 50%·70% cut을 공식 표로 함께 확인하지 못해 추가하지 않았다. 확인 가능한 공식 표가 확보되면 다음 단위에서 추가한다.
- `index.html`, `styles.css`, 그리고 사용자 수정 중인 `src/app.mjs`는 변경·스테이징하지 않았다.

### 경기도 공식 결과 추가

- 가천대학교 입학처가 2026-05-29에 게시한 `2026학년도 수시모집 입시결과.xlsx`에서 학생부우수자전형의 50%·70% cut을 확인했다. 약학과·의예과를 포함한 15개 모집단위/전형을 추가했다.
- 가천대학교 기본정보와 어디가 2027학년도 페이지 URL을 추가하고, 결과를 `src/admission-results/2026/gyeonggi.mjs` 및 dynamic import 목록에 연결했다.
- 추가 결과는 모두 원본 9등급 수치와 5등급 환산 참고값을 병행 보존한다. UI·비교 로직·`styles.css`는 변경하지 않았다.

## 서울·경기·인천 대학 기본정보 전체 감사 1차

- 대입정보포털 어디가 대학정보 목록(공식 대학·캠퍼스 220건)을 기준으로 서울·경기·인천 대상 72개 대학 기본정보를 `src/data/universities.mjs`에 등록했다. 대학 기본정보 등록과 입시결과 확보 여부를 분리했다.
- 기존 가천대학교·인천대학교 결과를 제외한 대학은 공식 2026 학생부교과 50%·70% cut을 확인하기 전까지 결과 파일에 임의 추가하지 않고 `admissionResultsAvailable: false`로 유지한다.
- `src/data/university-audit-2026.mjs`에 지역별 감사 행과 요약을 추가했다. 어디가 URL은 공식 상세 페이지에서 확인했으며, 홈페이지·입학처 URL은 실제 확인 전 null/미확인으로 보존한다.
- 공식 cut이 평균값만 제공되거나 확인되지 않은 대학은 cut으로 변환하지 않았다. 부산교육청 5등급 환산 로직과 기존 UI·`styles.css`는 변경하지 않았다.
- 구조·입결·환산·내신·카탈로그 회귀 테스트와 `git diff --check`를 통과했다.

## 학생 화면 UI/UX 최종 보정

- 상단 작업 버튼을 결과표 인쇄/초기화, JSON 내보내기/불러오기의 2×2 배치로 고정하고 모바일에서도 동일한 높이와 한 줄 문구를 유지하도록 보완했다.
- 학생 화면의 선생님 링크를 노출하지 않고, 제목 앞 아이콘 5회 연속 클릭·탭(2.5초 내)으로만 선생님 페이지에 진입하도록 동작을 실제 브라우저에서 확인했다. 선생님 화면의 학생 페이지 복귀도 확인했다.
- 히어로 제목의 반응형 글자 크기와 줄 간격을 조정하고, 제목·버튼은 둥근 글꼴, 본문·입력값은 읽기 쉬운 본문 글꼴로 분리했다.
- 전년도 입시결과 참고 영역의 기준 선택, 등급제 토글, 조회 버튼, 필터와 안내문을 계층화하고 모바일에서는 자연스럽게 세로로 쌓이도록 정리했다.
- 로컬 `127.0.0.1:5500`에서 새로고침 후 학생 화면과 입시결과 영역을 확인했다. 계산·입결·환산·저장 로직은 수정하지 않았다.
- 전체 회귀 테스트와 `git diff --check`를 통과했다.

## 학생 화면 반응형 타이포그래피 시스템

- `styles.css`에 브랜드, 히어로, 섹션, 카드, 본문, 설명, 라벨, 버튼, 결과 숫자용 공통 `clamp()` 토큰을 정의했다.
- 같은 역할의 제목·본문·보조문구가 동일한 토큰과 line-height를 사용하도록 학생 화면 선택자를 정리했다. 모바일에서는 최소 가독성 크기를 유지하고, 큰 화면에서는 상한을 두어 제목만 과도하게 커지지 않게 했다.
- 제목에는 친근한 글꼴을 유지하고 본문·입력값·설명은 읽기 쉬운 본문 글꼴을 사용하도록 분리했다. 한국어 줄바꿈은 `keep-all`을 유지했다.
- 로컬 브라우저에서 새로고침 후 상단·히어로·교과군·입시결과 영역을 두 차례 확인했다. 360~430px 모바일, 600~768px 태블릿, 1024px 이상 데스크톱에 대응하는 미디어 토큰을 점검했다.
- 기능 로직은 변경하지 않았고 전체 회귀 테스트와 `git diff --check`를 통과했다.

## 학생 화면 히어로 영역 보정

- 히어로 제목을 `clamp(20px, 1.7vw, 23px)`로 낮춰 작은 창에서 과도하게 커지지 않도록 했다.
- 데스크톱·태블릿은 텍스트:장식 영역을 약 2.8:0.55로 조정하고 장식 최대 폭을 120px로 줄였다.
- 모바일에서는 히어로를 1열로 전환하고 꽃 장식을 숨겨 제목이 전체 폭을 사용하도록 했다.
- 히어로 padding·gap·line-height를 함께 조정하고 로컬 브라우저에서 새로고침 후 2차례 확인했다.
- 기능·계산·데이터 구조는 변경하지 않았으며 전체 회귀 테스트와 `git diff --check`를 통과했다.

## 태블릿 입력 사용성 보정

- 768~1199px 구간에 태블릿 전용 터치 레이아웃을 추가했다.
- 과목 등급·성취도 select, 입력창, 삭제 버튼, 선택 과목 추가, 목표 시뮬레이션 버튼을 최소 48px 높이와 15px 글자로 확대했다.
- 과목 카드 간격과 내부 padding을 늘리고, 학교 개설 과목 선택 영역은 select와 추가 버튼을 충분한 폭의 2열로 재배치했다.
- 입력 안내·목표 안내 문구는 태블릿에서 14px과 넉넉한 line-height를 사용하도록 조정했다.
- 연결된 로컬 브라우저에서 과목 입력 카드, 과목 추가 영역, STEP 3 목표 시뮬레이션 영역을 확인했고 회귀 테스트와 `git diff --check`를 통과했다.

## 입시결과 비교 영역 구조 개선

- 기존 `전형`과 `전형 유형`이 데이터상 같은 값으로 반복되던 문제를 확인했다. 필터를 `전형명`(예: 일반전형)과 `전형 구분`(학생부교과/학생부종합)으로 분리했다.
- `admissionName` 필터를 연결하고 기존 `admissionType` 호환 조건은 유지해 검색 로직의 기존 동작을 보존했다.
- `내신 계산 후 이용 가능`, 비교 기준, 표시 등급제, 조회 버튼을 상태·선택·실행 영역으로 나눠 카드형으로 재배치했다.
- 모바일 화면에서 각 영역이 세로로 쌓이고, 필터를 열었을 때 전형명/전형 구분이 명확히 보이는 것을 실제 브라우저에서 확인했다.
- 계산·입결·환산·저장 구조는 변경하지 않았으며 전체 회귀 테스트와 `git diff --check`를 통과했다.

## 지역인재 전형 부산 학교 지원자격 확인

- 지역인재 결과 행을 전부 기본 숨김으로 처리하던 방식을 보완했다. `regionalEligibility`에 지원 가능 권역·고교 소재지 권역·공식 지원자격 요약·출처 URL·확인 여부·확인일을 분리해 저장한다.
- 학교 기본 소재지는 `부산`으로 두고, 공식 자료에서 부산 고교의 지원 가능이 확인된 경우에만 기본 학생 검색에 포함한다. 확인되지 않은 지역인재 전형은 계속 숨긴다.
- 현재 17개 지역인재 결과 행(7개 대학·전형 묶음)은 울산대, 경상국립대, 국립부경대, 동아대, 고신대, 인제대, 경남대의 공식 자료에서 부산·울산·경남 고교 전 교육과정 이수 요건을 확인했다. 이 중 울산대 의예과와 경상국립대 간호학과는 중학교 이수·거주 등 학생별 추가 조건도 있어, 현재 저장 정보만으로 확인할 수 없으므로 기본 검색에서 제외했다.
- 원본 입시결과 값·5등급 환산·UI/CSS는 변경하지 않았다. 지역인재 지원자격 단위 테스트와 전체 회귀 테스트를 실행한다.

## 전국 공식 입시결과 감사 · 서울 체크포인트

- 대입정보포털 어디가의 서울 지역 일반대학 목록 43개 대학·캠퍼스를 확정하고, 각 공식 2026학년도 전형평가기준 및 결과공개 페이지를 모두 확인했다.
- 대학 단위 학생부교과·학생부종합 상태를 실제 결과 레코드와 분리해 저장했다. 서울은 43개 모두 감사 완료했으며 교과 cut 확인 12개, 종합 내신 cut 확인 11개다.
- 전형명·모집단위·학생부등급 50%/70% 열을 명확히 식별할 수 있는 공식 표만 반영해 1,282건(교과 427, 종합 855)을 추가했다. 통합표의 전형명이 불명확하거나 동일 키의 공식 값이 충돌하는 행은 추측하지 않고 제외 사유를 감사 기록에 남겼다.
- 모든 신규 cut은 9등급 원본을 보존하고 기존 부산교육청 공식 실측 대응표 기반 5등급 참고값을 생성한다. 자격 제한 전형은 기본 검색에서 숨기는 기존 분류를 유지한다.
- 재개용 상세 체크포인트는 `progress.md`에 기록했다. `index.html`과 `styles.css`의 사용자 변경은 건드리지 않았다.
- 전체 회귀 테스트 40개를 통과했다.

## 전국 공식 입시결과 감사 · 경기 체크포인트

- 공식 어디가 경기 지역 일반대학 목록 36개 대학·캠퍼스를 확정하고 전부 감사했다.
- 전형명과 학생부등급 열이 명확한 표에서 550건(학생부교과 208, 학생부종합 342)을 새로 반영했다. 기존 가천대학교 입학처 15건을 포함한 경기 결과는 565건이다.
- 학생부교과 cut 보유 대학은 기존 가천대를 포함해 10개, 학생부종합 내신 cut 보유 대학은 4개다. 나머지는 공식 페이지를 확인했으나 자동 반영 가능한 50%/70%·평균등급 표가 없어 `not-published`로 기록했다.
- 경기대학교의 전형명이 없는 통합표 3개는 임의로 전형명을 붙이지 않고 제외 사유를 감사 기록과 `progress.md`에 남겼다.
- 기존 9등급 원본, 부산교육청 환산, 자격 제한 필터와 사용자 UI 파일을 그대로 유지했으며 전체 회귀 테스트 42개를 통과했다.

## 전국 공식 입시결과 감사 · 인천 체크포인트

- 공식 어디가 인천 지역 일반대학 목록 6개 대학·캠퍼스를 확정하고 모두 감사했다.
- 전형명과 학생부등급 열이 명확한 표에서 180건(학생부교과 101, 학생부종합 79)을 새로 반영했다. 기존 인천대학교 입학처와 인하대학교 공식 결과를 포함한 인천 결과는 213건이다.
- 학생부교과 cut 보유 대학 3개, 학생부종합 내신 cut 보유 대학 2개로 확인했다. 나머지 대학은 공식 페이지 확인 결과 공개 cut·평균등급을 안전하게 추출할 표가 없어 `not-published`로 기록했다.
- 청운대학교는 공식 어디가 지역 목록상 충남으로 확인되어 기존 기본정보의 지역을 바로잡았다. UI 파일은 건드리지 않았고 전체 회귀 테스트 44개를 통과했다.

## 전국 공식 입시결과 감사 · 부산 체크포인트

- 공식 어디가 부산 지역 일반대학 목록 14개를 확정하고 모두 감사했다.
- 명확한 전형명·모집단위·학생부등급 표에서 303건을 새로 반영했으며 기존 공식 확인 36건을 포함한 부산 결과는 339건(교과 167, 종합 172)이다.
- 학생부교과 cut 보유 대학 6개, 학생부종합 내신 cut 보유 대학 4개로 정리했다. 나머지는 공식 페이지 확인 후 `not-published`로 기록했다.
- 신규 지역인재 전형은 지원권역을 별도로 검증하지 않은 경우 빈 자격 메타데이터와 `verified: false`를 저장해 학생 기본 검색에서 노출하지 않는다.
- 전체 회귀 테스트 45개를 통과했고 사용자 UI 파일은 건드리지 않았다.

## 전국 공식 입시결과 감사 · 울산 체크포인트

- 공식 어디가 일반대학 울산 목록의 울산대학교 1개교를 감사 완료했다.
- 공식 어디가 상세 페이지에는 안전하게 추출할 수 있는 신규 cut 표가 없어 새 결과는 추가하지 않았고, 기존 공식 확인 학생부교과 결과 4건을 그대로 유지했다.
- 대학 감사 상태는 기존 공식 레코드를 반영해 학생부교과 `confirmed-cut`, 학생부종합 `not-published`로 정리했다.
- 전체 회귀 테스트 46개를 통과했고 사용자 UI 파일은 건드리지 않았다.

## 전국 공식 입시결과 감사 · 경남 체크포인트

- 공식 어디가 경남 일반대학 목록 11개 대학·캠퍼스를 확정하고 모두 감사했다.
- 명확한 전형명·모집단위·학생부등급 표에서 280건(학생부교과 160, 학생부종합 120)을 새로 반영했다. 기존 공식 확인 결과를 포함한 경남 결과는 307건이다.
- 학생부교과 cut 보유 대학 5개, 학생부종합 내신 cut 보유 대학 2개로 정리했으며, 공식 페이지에서 안전하게 추출할 수 없는 대학은 `not-published`로 기록했다.
- 원본 9등급 값과 기존 부산교육청 환산을 분리 보존하고, 미검증 지역인재는 기본 검색에서 숨긴다.
- 전체 회귀 테스트 47개를 통과했고 사용자 UI 파일은 건드리지 않았다.

## 전국 공식 입시결과 감사 · 대구 체크포인트

- 공식 어디가 대구 일반대학 목록 3개교를 확정하고 모두 감사했다.
- 계명대학교와 대구교육대학교의 명확한 공식 결과 표에서 318건(학생부교과 165, 학생부종합 153)을 반영했다.
- 경북대학교는 공식 페이지를 확인했으나 자동 반영 가능한 모집단위별 cut·평균등급 표가 없어 `not-published`로 기록했다.
- 신규 지역 모듈과 lazy loading 경로를 연결했고 원본 9등급 값·부산교육청 5등급 환산값을 분리 보존했다.
- 전체 회귀 테스트 48개를 통과했고 사용자 UI 파일은 건드리지 않았다.

## 전국 공식 입시결과 감사 · 경북 체크포인트

- 공식 어디가 경북 일반대학 목록 18개교를 확정하고 모두 감사했다.
- 전형명·모집단위·학생부등급 열을 명확히 식별한 공식 표에서 319건(학생부교과 223, 학생부종합 96)을 반영했다.
- 학생부교과 cut 보유 대학 4개, 학생부종합 내신 cut 보유 대학 4개이며 데이터 상태는 `confirmed-cut` 244건, `cut50-only` 67건, `cut70-only` 8건이다.
- 원본 9등급 값과 기존 부산교육청 환산을 분리 보존하고, 자격이 확인되지 않은 전형은 학생 기본 검색에서 숨긴다.
- 전체 회귀 테스트 49개를 통과했고 사용자 UI 파일은 건드리지 않았다.

## 전국 공식 입시결과 감사 · 대전 체크포인트

- 공식 어디가 대전 일반대학 목록 11개 대학·캠퍼스를 확정하고 모두 감사했다.
- 전형명·모집단위·학생부등급 열이 명확한 공식 표에서 405건(학생부교과 240, 학생부종합 165)을 반영했다.
- 학생부교과 cut 보유 대학 3개, 학생부종합 내신 cut 보유 대학 3개로 정리했다.
- 배재대학교·한남대학교의 전형명 미식별 종합 표와 충남대학교의 충돌 키는 추측하지 않고 제외 사유를 감사 기록에 남겼다.
- 전체 회귀 테스트 50개를 통과했고 사용자 UI 파일은 건드리지 않았다.

## 전국 공식 입시결과 감사 · 세종 체크포인트

- 공식 어디가 세종 일반대학 목록 3개 대학·캠퍼스를 확정하고 모두 감사했다.
- 고려대학교(세종)의 명확한 공식 결과 표에서 학생부교과 25건을 반영했으며, 공개된 값에 맞춰 모두 `cut50-only`로 보존했다.
- 나머지 대학은 공식 페이지를 확인했으나 자동 반영 가능한 모집단위별 cut·평균등급 표가 없어 `not-published`로 기록했다.
- 신규 지역 모듈과 lazy loading 경로를 연결했고 전체 회귀 테스트 51개를 통과했다. 사용자 UI 파일은 건드리지 않았다.

## 전국 공식 입시결과 감사 · 충남 체크포인트

- 공식 어디가 충남 일반대학 목록 16개 대학·캠퍼스를 확정하고 모두 감사했다.
- 안전하게 식별한 공식 결과 242건(학생부교과 157, 학생부종합 85)을 반영했다. 상태는 `confirmed-cut` 121건, `cut50-only` 121건이다.
- 학생부교과 cut 보유 대학 5개, 학생부종합 내신 cut 보유 대학 2개로 정리했다.
- 한국기술교육대학교의 충돌 키 2건과 호서대학교의 전형명 미식별 통합표 5개는 추측 없이 제외하고 감사 기록에 사유를 남겼다.
- 전체 회귀 테스트 52개를 통과했고 사용자 UI 파일은 건드리지 않았다.

## 전국 공식 입시결과 감사 · 충북 체크포인트

- 공식 어디가 충북 일반대학 목록 12개 대학·캠퍼스를 확정하고 모두 감사했다.
- 안전하게 식별한 공식 결과 188건(학생부교과 157, 학생부종합 31)을 반영했다. 상태는 `confirmed-cut` 165건, `cut50-only` 23건이다.
- 학생부교과 cut 보유 대학 2개, 학생부종합 내신 cut 보유 대학 2개로 정리했다.
- 전형명이 없는 통합표는 대학별 전형을 추측하지 않고 제외 사유를 감사 기록에 남겼다.
- 전체 회귀 테스트 53개를 통과했고 사용자 UI 파일은 건드리지 않았다.

## 전국 공식 입시결과 감사 · 광주 체크포인트

- 공식 어디가 광주 일반대학 목록 10개교를 확정하고 모두 감사했다.
- 안전하게 식별한 공식 결과 694건(학생부교과 479, 학생부종합 215)을 반영했다. 상태는 `confirmed-cut` 206건, `cut50-only` 488건이다.
- 학생부교과 cut 보유 대학 3개, 학생부종합 내신 cut 보유 대학 3개로 정리했다.
- 지원자격 제한 전형과 미확인 전형은 공식 수치를 보존하면서 학생 기본 검색에서 숨기는 기존 정책을 유지했다.
- 전체 회귀 테스트 54개를 통과했고 사용자 UI 파일은 건드리지 않았다.

## 전국 공식 입시결과 감사 · 전남 체크포인트

- 공식 어디가 전남 일반대학 목록 10개 대학·캠퍼스를 확정하고 모두 감사했다.
- 전남대학교 제2캠퍼스 페이지에서 추출된 372건이 광주 본교 페이지의 결과와 완전히 동일하여 캠퍼스별 모집단위 귀속을 확인할 수 없음을 검증했다.
- 같은 공식 값을 전남 결과로 복제하지 않도록 수집 안전장치를 추가하고, 해당 캠퍼스를 `completed-with-parser-warnings`로 기록했다. 전남에서 안전하게 반영한 결과는 0건이다.
- 나머지 대학도 공식 페이지를 확인했으나 자동 반영 가능한 모집단위별 cut·평균등급 표가 없어 `not-published`로 보존했다.
- 전체 회귀 테스트 55개를 통과했고 사용자 UI 파일은 건드리지 않았다.

## 전국 공식 입시결과 감사 · 전북 체크포인트

- 공식 어디가 전북 일반대학 목록 10개교를 확정하고 모두 감사했다.
- 안전하게 식별한 공식 결과 241건(학생부교과 152, 학생부종합 89)을 반영했다. 상태는 `confirmed-cut` 89건, `cut50-only` 152건이다.
- 학생부교과 cut 보유 대학 2개, 학생부종합 내신 cut 보유 대학 2개로 정리했다.
- 원본 9등급 값·부산교육청 5등급 환산·지원자격 필터 정책을 유지했고 전체 회귀 테스트 56개를 통과했다. 사용자 UI 파일은 건드리지 않았다.

## 전국 공식 입시결과 감사 · 강원 체크포인트

- 공식 어디가 강원 일반대학 목록 14개 대학·캠퍼스를 확정하고 모두 감사했다.
- 안전하게 식별한 공식 결과 464건(학생부교과 368, 학생부종합 96)을 반영했다. 상태는 `confirmed-cut` 96건, `cut50-only` 368건이다.
- 학생부교과 cut 보유 대학 3개, 학생부종합 내신 cut 보유 대학 1개이며, 전형 자체가 확인되지 않은 대학도 별도 상태로 보존했다.
- 원본 9등급 값·부산교육청 5등급 환산·지원자격 필터 정책을 유지했고 전체 회귀 테스트 57개를 통과했다. 사용자 UI 파일은 건드리지 않았다.

## 전국 공식 입시결과 감사 · 제주 및 최종 체크포인트

- 공식 어디가 제주 일반대학 목록 2개교를 확정하고 모두 감사했다. 안전하게 반영 가능한 모집단위별 cut·평균등급 표는 없어 결과를 임의 생성하지 않았다.
- 전국 17개 지역의 공식 일반대학 목록 220개 대학·캠퍼스를 모두 감사 상태로 정리했다. 미확인·수집 실패 대학은 없다.
- 입시결과는 총 5,606건이며 학생부교과 3,106건, 학생부종합 2,500건이다. 모든 행은 공식 출처 URL·2026 기준 학년도·전형 구분을 갖고 원본 9등급과 부산교육청 기준 5등급 환산을 분리 보존한다.
- 전국 데이터의 canonical 키 중복, 환산 범위, 모든 지역 lazy loading을 검증하는 회귀 테스트를 추가했다.
- 최종 회귀 테스트 60개를 통과했고 사용자 수정 중인 `index.html`과 `styles.css`는 변경·스테이징하지 않았다.

## 서울 공식 입학처 심층 재감사 · 삼육대학교

- 대입정보포털 표에 cut이 없던 삼육대학교를 공식 입학처의 2026학년도 수시 최종등록자 결과표로 재점검했다.
- 공식 표에서 학생부교과 학교장추천 20건, 학생부종합 세움인재·S/W인재 22건의 학생부 평균등급을 확인해 총 42건을 추가했다.
- 공개 수치는 50%/70% cut이 아닌 평균등급이므로 `averageGradeOriginal`과 `averageGradeConverted`에만 저장하고 모든 레코드를 `average-only`로 분류했다.
- 세움인재·S/W인재의 지원자격은 전형명만으로 단정하지 않고 `unknown`을 유지했다. 학교장추천만 명확한 자격 유형으로 분류했다.
- 전체 입시결과는 5,648건으로 늘었고 회귀 테스트 61개가 모두 통과했다.
- 사용자 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 서울 모집단위별 2026 공식 입시결과 재감사

- 어디가 대학 요약 페이지의 조회연도와 실제 결과연도 표기가 다름을 확인했다. `searchSyr=2026` 대학 요약 페이지에는 2025학년도 전형 결과가 포함될 수 있어, 이를 2026 결과로 계속 사용하지 않도록 서울부터 교정했다.
- 공식 어디가 모집단위별 결과 팝업은 `searchSyr=2026`에서 2026학년도 결과를 직접 표시하므로, 43개 대학의 설치학과 페이지를 모두 순회해 팝업의 학생부 환산등급 50%·70% cut을 수집했다.
- 서울 43개 대학 감사를 완료했고 42개 대학에서 2,931건의 공식 cut 레코드를 확보했다. 삼육대 입학처 평균등급을 병합한 활성 서울 데이터는 총 2,933건이다.
- 활성 서울 데이터는 학생부교과 957건, 학생부종합 1,976건이다. 서경대학교는 공식 팝업에서 공개 cut 레코드를 확인하지 못했다.
- 학생부교과/학생부종합 전형명을 `학교장추천`, `세움인재`처럼 정규화했지만 카테고리 필드는 별도로 유지했다.
- 광운대 2개, 상명대 1개 충돌 키는 어느 값을 선택할 근거가 없어 제외하고 감사 경고에 남겼다.
- 기존 생성 파일은 삭제하지 않고 보존했으며 새 `verified` 파일을 서울 서비스 데이터로 연결했다.
- 전체 회귀 테스트 61개가 모두 통과했고 사용자 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 경기 모집단위별 2026 공식 입시결과 재감사

- 어디가 대학 요약표 대신 `searchSyr=2026` 모집단위별 공식 팝업을 사용해 경기 36개 대학·캠퍼스를 전수 재감사했다.
- 36개 전체 감사에 성공했으며 활성 경기 데이터는 1,913건(학생부교과 1,229건, 학생부종합 684건)이다.
- 예원예술대학교(제2캠퍼스)와 중앙승가대학교는 공식 cut 레코드가 없으며, 한국공학대학교의 충돌 canonical 키 1개는 추측 없이 제외했다.
- 가천대학교 기존 임시 값은 공식 자료상 학생부종합 `가천바람개비`에 해당함을 확인해 활성 분류를 교정했다. 기존 원본 파일은 삭제하거나 덮어쓰지 않았다.
- 원본 9등급 값과 부산교육청 기준 5등급 환산값을 분리 보존했고, 전체 회귀 테스트 61개가 모두 통과했다.
- 사용자 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 인천 모집단위별 2026 공식 입시결과 재감사

- 인천 6개 대학·캠퍼스의 어디가 모집단위별 2026 공식 팝업을 전수 확인해 373건(학생부교과 159건, 학생부종합 214건)을 반영했다.
- 6개 대학 모두 학생부교과 cut을 확인했고, 4개 대학은 학생부종합 cut도 확인했다. 수집 실패와 충돌 제외는 없다.
- 인천대학교의 국어국문학과 교과성적우수자전형처럼 기존 입학처 70% cut과 일치하면서 50% cut도 공식 확인되는 모집단위 자료를 연결했다.
- 인하대학교 농어촌학생 자료는 데이터에서 삭제하지 않고 `rural`로 보존하며 학생 기본 검색에서 숨기는 정책을 유지했다.
- `자기추천전형`을 학교장추천으로 오인하던 정규식 순서를 교정하고 회귀 테스트로 고정했다.
- 전체 회귀 테스트 61개가 모두 통과했고 사용자 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 부산 모집단위별 2026 공식 입시결과 재감사

- 부산 14개 대학·캠퍼스의 공식 2026 모집단위별 결과를 전수 확인해 1,814건(학생부교과 1,251건, 학생부종합 563건)을 반영했다.
- 학생부교과 결과 대학 13개, 학생부종합 결과 대학 12개이며 수집 실패는 없다.
- 동서대 4개, 동의대 3개, 부산외대 1개, 신라대 1개의 충돌 canonical 키는 추측 없이 제외했다.
- 기존 공식 모집요강 감사에서 부산 학생 지원 가능성이 확인된 지역인재 7건의 자격정보를 새 모집단위별 cut에 안전하게 병합했다.
- 기존 원본 파일은 보존했고 원본 9등급·부산교육청 기준 5등급 환산·특별전형 기본 숨김 정책을 유지했다.
- 전체 회귀 테스트 62개가 통과했고 사용자 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 울산 모집단위별 2026 공식 입시결과 재감사

- 공식 어디가 일반대학 목록의 울산대학교를 전수 확인해 모집단위별 cut 72건(학생부교과 46건, 학생부종합 26건)을 확보했다.
- 설치학과 상세 목록에 없는 울산대학교 의예과 지역교과 기존 공식 결과 1건은 새 결과와 충돌하지 않으므로 삭제하지 않고 보존했다.
- 의예과 지역교과의 공식 지원지역과 추가 개인 확인 요건을 유지해 자동 노출하지 않는다.
- 활성 울산 데이터는 총 73건이며 원본 9등급·5등급 환산을 분리 보존한다.
- 전체 회귀 테스트 63개가 통과했고 사용자 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 경남 모집단위별 2026 공식 입시결과 재감사

- 경남 11개 대학·캠퍼스를 전수 확인해 공식 모집단위별 결과 694건(학생부교과 475건, 학생부종합 219건)을 확보했다.
- 새 설치학과 목록에 없지만 기존 공식 자료에서 확인된 3건은 충돌하지 않아 보존했으며 활성 데이터는 총 697건이다.
- 인제대 1개와 창신대 4개의 충돌 canonical 키는 임의 선택하지 않고 제외했다.
- 부산 학생 지원 가능성이 공식 확인된 지역인재 9건의 자격정보를 새 결과에 병합하고, 나머지 regional 전형은 기본 숨김을 유지했다.
- 전체 회귀 테스트 64개가 통과했고 사용자 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 대구 모집단위별 2026 공식 입시결과 재감사

- 대구 3개 대학을 전수 확인해 공식 모집단위별 결과 623건(학생부교과 331건, 학생부종합 292건)을 반영했다.
- 경북대·계명대는 학생부교과와 종합 결과를, 대구교대는 학생부종합 결과를 공식 상세표로 확인했다.
- 수집 실패·충돌 제외가 없고 모든 행의 공식 출처와 2026 기준 학년도를 검증했다.
- 전체 회귀 테스트 65개가 통과했고 사용자 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 경북 모집단위별 2026 공식 입시결과 재감사

- 경북 18개 대학·캠퍼스를 전수 감사해 모집단위별 공식 결과 1,353건(학생부교과 1,009건, 학생부종합 344건)을 반영했다.
- 공식 cut이 없는 대학은 대학 감사 상태로만 남기고 결과 레코드를 임의 생성하지 않았다.
- 5개 대학의 충돌 canonical 키 17개는 서로 다른 공식 값 중 하나를 임의 선택하지 않고 제외했다.
- 전체 회귀 테스트 66개가 통과했고 사용자 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 대전 모집단위별 2026 공식 입시결과 재감사

- 대전 11개 대학·캠퍼스를 전수 감사해 모집단위별 공식 결과 1,117건(학생부교과 801건, 학생부종합 316건)을 반영했다.
- 모든 대학에서 학생부교과 cut을 확인했고 7개 대학에서 학생부종합 내신 cut을 확인했다.
- 수집 실패·충돌 제외가 없으며 전체 회귀 테스트 67개가 통과했다.
- 사용자 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 세종 모집단위별 2026 공식 입시결과 재감사

- 세종 3개 대학·캠퍼스를 전수 확인해 모집단위별 공식 결과 82건(학생부교과 41건, 학생부종합 41건)을 반영했다.
- 수집 실패와 충돌 제외가 없으며 공식 출처 URL과 2026 기준 학년도를 모두 확인했다.
- 전체 회귀 테스트 68개가 통과했고 사용자 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 충남 모집단위별 2026 공식 입시결과 재감사

- 충남 16개 대학·캠퍼스를 전수 감사해 모집단위별 공식 결과 1,573건(학생부교과 1,181건, 학생부종합 392건)을 반영했다.
- 호서대학교의 충돌 canonical 키 2개는 임의 선택하지 않고 제외했으며 수집 실패는 없다.
- 전체 회귀 테스트 69개가 통과했고 사용자 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 충북 모집단위별 2026 공식 입시결과 재감사

- 충북 12개 대학·캠퍼스를 전수 감사해 모집단위별 공식 결과 1,097건(학생부교과 840건, 학생부종합 257건)을 반영했다.
- 수집 실패·충돌 제외가 없고 전체 회귀 테스트 70개가 통과했다.
- 사용자 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 광주 모집단위별 2026 공식 입시결과 재감사

- 광주 10개 대학을 전수 감사해 모집단위별 공식 결과 668건(학생부교과 484건, 학생부종합 184건)을 반영했다.
- 3개 대학의 충돌 canonical 키 각 1개는 임의 선택하지 않고 제외했으며 수집 실패는 없다.
- 전체 회귀 테스트 71개가 통과했고 사용자 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 전남 모집단위별 2026 공식 입시결과 재감사

- 전남 10개 대학·캠퍼스를 전수 감사해 모집단위별 공식 결과 319건(학생부교과 256건, 학생부종합 63건)을 반영했다.
- 기존 요약표에서 본교와 중복돼 제외했던 전남대 제2캠퍼스는 새 학과별 상세 팝업에서 캠퍼스 고유 결과 31건을 확인했다.
- 3개 대학의 충돌 canonical 키 10개는 임의 선택 없이 제외했고 전체 회귀 테스트 72개가 통과했다.
- 사용자 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 전북 모집단위별 2026 공식 입시결과 재감사

- 전북 10개 대학을 전수 감사해 모집단위별 공식 결과 589건(학생부교과 385건, 학생부종합 204건)을 반영했다.
- 4개 대학의 충돌 canonical 키 22개는 임의 선택 없이 제외했고 수집 실패는 없다.
- 전체 회귀 테스트 73개가 통과했고 사용자 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 강원 모집단위별 2026 공식 입시결과 재감사

- 강원 14개 대학·캠퍼스를 전수 감사해 모집단위별 공식 결과 916건(학생부교과 586건, 학생부종합 330건)을 반영했다.
- 상지대 12개와 한라대 3개의 충돌 canonical 키는 임의 선택하지 않고 제외했으며 수집 실패는 없다.
- 전체 회귀 테스트 74개가 통과했고 사용자 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 제주 및 전국 모집단위별 2026 공식 결과 재검증 완료

- 제주 2개 대학을 감사해 제주대학교 공식 결과 119건(학생부교과 101건, 학생부종합 18건)을 반영했다. 제주국제대학교는 공개 cut이 없어 임의 데이터를 생성하지 않았다.
- 전국 17개 지역 220개 대학·캠퍼스의 공식 모집단위별 상세 결과 재검증을 완료했다.
- 활성 결과는 총 16,259건이며 학생부교과 10,136건, 학생부종합 6,123건이다. `confirmed-cut` 16,257건과 삼육대 공식 `average-only` 2건으로 구성된다.
- 공식 출처 URL·기준 학년도·전형 카테고리 누락과 원본 cut의 5등급 환산값 누락이 모두 0건임을 확인했다.
- 학생부종합 비비교, 특별전형 기본 숨김, 부산교육청 실측 대응표 환산, 지역 lazy loading 정책을 유지했다.
- 최종 회귀 테스트 75개가 모두 통과했고 사용자 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 입시결과 종속형 검색 필터 적용

- 지역 → 대학 → 계열 → 모집단위 → 전형명 → 전형 구분의 현재 조건에 맞춰 선택지를 다시 계산하도록 필터 선택지 생성 로직을 별도 모듈로 분리했다.
- 상위 조건을 바꿨을 때 더 이상 유효하지 않은 하위 선택은 자동으로 전체 선택으로 돌아가며, 대학을 고르기 전에는 전국 모집단위를 긴 드롭다운으로 만들지 않는다.
- 전국 모집단위 검색용 `searchAvailableDepartments`와 대학·계열·모집단위·전형명별 재사용 가능 선택지 함수를 추가했다. 공식 모집단위명은 그대로 반환하고 검색 보조 메타데이터만 별도로 계산한다.
- 공식 계열값이 없는 자료는 모집단위명으로 계열이 명확한 경우에만 `academicField`를 보완하고, 애매한 이름은 `unknown`으로 유지한다.
- 특별전형 기본 숨김, 부산 지역인재 자격, 학생부교과/종합 구분, 9등급 원본/5등급 환산, 관심 대학 및 지역 lazy loading 로직은 유지했다.
- 표준 회귀 테스트 82개와 문법·diff 검사를 통과했고 로컬 서버의 페이지와 새 필터 모듈이 모두 HTTP 200으로 응답함을 확인했다.
- 사용자/Aside 수정 중인 `index.html`과 `styles.css`는 변경하지 않았다.

## 입시결과 모집단위 계열 분류 보완

- `academicField`를 `humanities`, `natural`, `arts`, `other`, `unknown`의 canonical 값으로 정리하고 공식 제공값을 모집단위명 보조 분류보다 우선하도록 했다.
- 공식 계열값이 없는 2026학년도 입시결과는 모집단위명으로 명확한 경우만 보조 분류했으며, 학제간·융합 모집단위처럼 확정하기 어려운 항목은 `unknown`으로 유지했다.
- 현재 16,259건은 인문 5,773건, 자연 8,586건, 예체능 972건, 기타 658건, 미분류 270건이다. 미분류 고유 모집단위 74개는 `docs/admission-academic-field-audit-2026.md`에 기록했다.
- 학생 필터에는 인문/자연/예체능/기타·미분류 선택지를 제공하며 기타·미분류 선택은 canonical `other`와 `unknown`을 함께 조회한다.
- 새 공식 데이터 생성 함수가 `academicField`를 직접 받을 수 있게 확장했고, 전체 분포를 재검증하는 `summarizeAdmissionAcademicFields`를 추가했다.
- 계열별 필터, 전체 계열, 다른 검색 조건과의 조합, 공식 계열 우선순위를 포함한 전체 회귀 테스트 84개가 통과했다.
- 사용자/Aside 수정 중인 `index.html`과 `styles.css`는 변경하지 않았다.

## 목표 내신 시나리오 학기 중복 출력 수정

- 상세 입력의 남은 과목 배열을 시나리오 화면에서 직접 반복하던 원인을 수정하고, 시뮬레이션 대상 5개 학기 범위 안에서 `semesterId`별 학점을 먼저 집계하도록 했다.
- 균형형·초반 집중형·후반 상승형 모두 과목이 아닌 학기 단위 목표를 사용하며, 초반/후반형의 집중 목표도 첫·마지막 과목이 아니라 첫·마지막 남은 학기 전체에 적용한다.
- 동일 학기에 여러 과목 목표가 있는 경우 실제 학점 가중 평균으로 하나의 학기 결과를 만들고 `1-1`, `1-2`, `2-1`, `2-2`, `3-1` 순서를 보장한다. 3학년 2학기는 제외한다.
- 자동 테스트 89개가 모두 통과했다. 실제 로컬 브라우저에서도 상세 과목 6개 조건으로 세 시나리오가 각각 남은 4개 학기만 한 줄씩 표시되고 예상 최종 내신이 한 번만 출력됨을 확인했다.
- 390px, 768px, 1024px, 1366px에서 시나리오 카드의 행 수와 가로 넘침을 확인했으며 모든 폭에서 가로 스크롤 없이 정상 표시됐다.
- 사용자/Aside 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 입시결과 대학 선택 목록을 대학 마스터 기준으로 수정

- 대학 선택 목록이 학생 기본 노출 입시결과 레코드에서 대학명을 추출하던 문제를 수정하고, `universities.mjs` 대학 마스터에서 지역 조건만 적용해 생성하도록 분리했다.
- 부산대학교는 `pusan-national`, `부산광역시`로 마스터와 237건의 입시결과에 정상 연결되어 있었지만, 결과 레코드의 지원자격이 기본 숨김 상태라 기존 목록에서 함께 누락되고 있었다. 부산교육대학교도 같은 이유로 누락됐다.
- 부산 지역 대학 선택 목록은 수정 전 12개에서 수정 후 마스터 전체 14개로 늘었고, 실제 로컬 브라우저에서 부산대학교와 부산교육대학교가 모두 표시됨을 확인했다.
- `부산`, `busan`, `부산광역시`처럼 입력 경로가 다른 지역값을 canonical 지역명으로 읽는 정규화 함수를 추가했다. 현재 대학 마스터 220건 자체에는 비표준 지역값이 없음을 확인했다.
- 전국 대학 마스터 220개 가운데 기존 기본 목록은 162개만 노출했으며, 수정 후 220개 모두 지역별 대학 목록에 포함된다. 공식 입시결과 레코드가 0건인 대학 20개도 더 이상 대학 목록에서 숨겨지지 않는다.
- 대학 선택 후 결과가 없으면 대학 감사 상태에 따라 `공식 미공개`, `아직 확인 중`, `공식 입시결과 미확보`, `해당 전형 결과 없음`을 구분해 안내한다.
- 대학 마스터 기반 부산대학교 노출, 결과 0건 대학 노출, 타 지역 제외, 중복 제거를 포함한 전체 회귀 테스트 91개가 모두 통과했다.
- 사용자/Aside 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 부산대학교 계열 필터 연결 복구

- 전체 입시결과 16,259건은 원본 생성 파일의 `academicField`·`field` 값이 비어 있어도 정규화 단계에서 모집단위명이 명확한 경우 canonical `academicField`를 보완하도록 되어 있음을 확인했다.
- 현재 분류는 인문 5,773건, 자연 8,586건, 예체능 972건, 기타 658건, 미분류 270건이다. 애매한 융합·자유전공 모집단위는 기타 또는 미분류로 유지한다.
- 부산대학교는 인문 75건, 자연 142건, 예체능 4건, 기타 10건, 미분류 6건으로 이미 분류돼 있었지만, 원본의 `eligibilityType: unknown`이 전형명 재분류를 막아 237건 전체가 계열 선택지 생성 전에 기본 숨김되던 것이 원인이었다.
- `unknown`을 확정 자격이 아닌 미분류 상태로 처리해 전형명이 명확한 경우에만 일반·농어촌·지역인재·특별전형으로 다시 분류했다. 부산대는 일반 158건이 기본 검색에 복원됐고 지역인재 57건, 농어촌 3건, 특별 18건, 불명확 1건은 기존 자격 노출 정책을 유지한다.
- 부산 전체 계열은 인문 627건, 자연 908건, 예체능 173건, 기타 78건, 미분류 28건이다.
- 실제 학생 화면에서 부산 → 부산대학교 선택 시 `전체 계열`, `인문`, `자연`, `예체능`, `기타/미분류`가 실제 데이터에 따라 생성됨을 확인했다.
- 계열 데이터 생성, 부산대 종속 선택지, 불명확 데이터 안전 처리, 상위 필터 변경 시 무효 계열 초기화를 포함한 전체 회귀 테스트 94개가 통과했다.
- 사용자/Aside 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 전국 대학 마스터·드롭다운·입시결과 연결 감사

- 프로젝트에 보존된 2026 대학어디가 지역별 공식 대학 목록 스냅샷 220개를 최종 대학 마스터와 독립된 감사 기준으로 내보내도록 정리했다.
- 전국 17개 지역에서 대학어디가 기준 220개, 대학 마스터 220개, 지역별 드롭다운 합계 220개가 모두 일치했다. 공식 목록의 마스터 누락, 마스터의 드롭다운 누락, 중복 대학, 지역 불일치는 모두 0건이다.
- 현재 입시결과를 보유한 대학은 200개이며, 입결 0건인 20개 대학도 대학 마스터를 기준으로 지역 드롭다운에 정상 노출된다.
- 입시결과 16,259건에서 `universityId` 누락, 대학 마스터 미연결, 이름 표기 불일치, 지역 불일치는 모두 0건으로 확인했다.
- 서울·부산·영문 지역 별칭을 17개 canonical 지역값으로 정규화하고, 실제 누락 ID·중복·지역 불일치를 탐지하는 재사용 가능 감사 모듈과 자동 테스트를 추가했다.
- 전체 회귀 테스트 101개가 모두 통과했다. 사용자/Aside 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 다중 학생 프로필 localStorage 구조 적용

- 기존 `naesin-simulator:v1` 단일 학생 저장을 학번별 `studentProfiles`, 현재 선택 학생 ID, 신규 학생 임시 입력으로 분리하는 저장 모듈을 추가했다.
- 학생 생성·현재 학생 저장·학생 전환·새 학생 시작·현재 입력 초기화·현재 학생 삭제·모든 학생 삭제·현재 프로필 조회 함수를 독립적으로 제공한다.
- 학생 전환 시 기존 학생을 먼저 저장하고 대상 학생을 불러오며, 동일 학번 재생성이나 학생번호 직접 변경으로 다른 학생 데이터가 덮어써지지 않게 보호했다.
- 관심 대학은 학생 프로필 안에 유지하고, 학교 공통 설정과 과목 카탈로그 저장 키는 학생 초기화 대상에서 제외했다.
- 기존 단일 저장값은 유효한 5자리 학번과 이름이 있으면 첫 프로필로 한 번만 자동 이전한다. 새 프로필 저장을 확인한 뒤 기존 키를 제거하고 이전 완료 표식을 남겨 중복 변환하지 않으며, 신원 정보가 미완성인 데이터는 임시 입력으로 보존한다.
- 기존 JSON 내보내기는 현재 학생 기준을 유지하고, JSON 불러오기는 다른 학생 프로필을 지우지 않은 채 해당 학번 프로필만 명시적으로 복원한다.
- 학생 A/B 독립 저장, 전환 복원, 입력 초기화, 학생 삭제, 전체 초기화, 관심 대학 분리, 공통 설정 유지, 기존 데이터 마이그레이션, 중복 학번 보호, JSON 복구를 포함한 전체 회귀 테스트 112개가 통과했다.
- 사용자/Aside 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 입시결과 기본 정렬·20건 단위 노출 적용

- 필터를 선택하지 않아도 학생부교과 기본 결과를 전국 공식 데이터에서 계산하되, 현재/목표 내신과 5등급 환산 70% cut의 절대 차이가 작은 순으로 정렬해 처음 20건만 DOM에 전달하도록 분리했다.
- 차이가 같으면 대학명, 모집단위, 전형명 순으로 안정 정렬하며, 지역·대학·계열·모집단위·전형명·자격 노출 조건은 기존 종속 필터 규칙을 그대로 적용한다.
- `visibleResultLimit`은 20에서 시작해 더 보기 요청마다 20씩 증가하고, 필터·현재 내신 재계산·목표 내신 변경·비교 기준 변경 시 20으로 초기화된다.
- 학생부종합은 현재/목표 내신 차이와 범위 분류를 만들지 않고 대학명·모집단위·전형명 순으로 정렬한 참고 자료만 20건씩 제공한다.
- 화면 연동용 `totalMatchedResults`, `visibleResults`, `visibleResultLimit`, `hasMore`, `remainingResultCount`를 반환하고 결과 컨테이너 dataset에도 동기화한다.
- 현재 공식 데이터의 기본 노출 대상은 학생부교과 4,212건, 학생부종합 2,084건이며 각각 첫 20건만 준비되는 것을 확인했다. 부산 교과 조건은 556건 중 20건이 준비된다.
- `cut70Original: null`이 숫자 0으로 오인되던 경계 사례를 수정해 공식 70% cut과 환산값이 모두 있는 교과 결과만 비교 정렬 대상으로 사용한다.
- 정렬, 동률 처리, 현재/목표 기준, 종속 필터 조합, 학종 비비교, 20건 단위 증가, null cut 제외를 포함한 전체 회귀 테스트 120개가 통과했다.
- 사용자/Aside 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다.

## 입시결과 교과·학종 최상위 탐색 모드 분리

- `student-record-subject`, `student-record-comprehensive` 두 값을 갖는 `admissionViewMode`를 일반 필터보다 위의 탐색 상태로 분리했다. 모드 선택 전에는 결과와 하위 필터 선택지를 만들지 않는다.
- 새 모드 컨트롤은 `[data-admission-view-mode]` 또는 `name="admission-view-mode"`로 연결할 수 있으며, 기존 전형 구분 select는 Aside 구조 전환 전 호환 입력으로만 처리한다.
- 교과는 부산교육청 대응표 기반 5등급 환산 70% cut과 현재/목표 내신을 비교해 `similar`, `higher`, `lower`로 분리한다. 학생 내신 2.00 기준 1.80·2.20은 ±0.20 경계에 포함되고 1.79는 입결이 높은 그룹, 2.21은 입결이 낮은 그룹으로 검증했다.
- 세 그룹 모두 절대 차이가 작은 순, 동률이면 대학명·모집단위·전형명 순으로 정렬하며 그룹별 20건 제한과 독립적인 더 보기 상태를 갖는다. 기본 펼침 상태는 `similar`이다.
- `subjectSimilarCount`, `subjectHigherCount`, `subjectLowerCount`와 그룹별 `totalCount`, `visibleResults`, `visibleResultLimit`, `hasMore`를 UI 연동값으로 제공한다.
- 학종은 현재/목표 내신과의 차이 및 교과 범위 그룹을 생성하지 않고 대학명·모집단위·전형명 순의 전년도 등록자 내신 참고 자료만 20건씩 제공한다.
- 하위 지역·대학·계열·모집단위·전형명 선택지는 현재 `admissionViewMode`에 해당하면서 학생 기본 노출 자격을 만족하는 데이터만으로 계산한다. 모드 변경 시 하위 필터와 그룹별 노출 수를 초기화한다.
- 실제 데이터에서 학생 내신 2.00 기준 교과 4,212건은 비슷한 범위 816건, 입결이 높은 범위 1,336건, 낮은 범위 2,060건이다. 학종 참고 자료는 2,084건이다.
- 9등급제 원본 모드에서는 교과 자료도 현재 내신과 직접 비교하거나 세 범위로 분류하지 않고, 전체 원본 자료를 가나다순으로 20건씩 제공한다.
- 모드 미선택, ±0.20 경계와 등급 방향, 교과 전용 차이, 학종 비비교, 원본 비비교, 모드별 필터 재계산, 그룹 정렬, 그룹별 20건 더 보기를 포함한 전체 회귀 테스트 124개가 통과했다.
- 사용자/Aside 수정 중인 `index.html`과 `styles.css`는 변경하거나 스테이징하지 않았다. 실행 시 기존 디자인 클래스를 재사용하는 최상위 `[학생부교과] / [학생부종합]` 선택 그룹을 생성하고, 의미가 중복되는 기존 `전형 구분` 필터는 숨겨 실제 화면에서도 탐색 순서를 분리했다.

## 학생부종합 첫 페이지 편중 및 부산대학교 누락 의심 감사

- 전국 학종 데이터는 6,123건·138개 대학이며, 기본 지원자격 검증 후 2,084건·57개 대학이 학생 화면 대상임을 확인했다.
- 부산대학교는 공식 학종 102건을 이미 보유하고 있으며 학생부종합전형 69건이 기본 노출 필터를 정상 통과한다. 어디가와 부산대학교 공식 2026 전형결과 게시도 대조했다.
- 가톨릭관동대학교가 화면 대부분처럼 보인 원인은 데이터 누락이 아니라 대학명 가나다순 결과를 20건에서 자르며 한 대학 모집단위가 첫 페이지를 점유한 것이었다.
- 학종 결과를 대학별 한 건씩 순환 배치해 첫 페이지의 대학 다양성을 보장했다. 부산 지역 조건 첫 20건에 부산대학교가 포함되는 것을 자동 검증했다.
- 전형 구분, 기본 지원자격, 지역인재 자격, 지역, 대학, 계열, 모집단위, 전형명 단계를 개별 집계하는 `traceAdmissionFilterPipeline`을 추가했다.
- 명시적 전형명과 `admissionCategory`가 서로 반대인 레코드는 0건이며, 공식 분류가 없는 전형을 이름만으로 재분류하지 않았다.
- 전체 대학별 현황과 필터 단계는 `docs/admission-comprehensive-audit-2026.md`에 기록했다.
- 전체 회귀 테스트 127개가 통과했다. 사용자/Aside 수정 중인 `index.html`과 `styles.css`는 변경하지 않았다.

## 입시결과 대학별 아코디언 데이터 및 학종 참고 구간 추가

- 교과의 기존 `classifySubjectAdmissionRange()`와 `similar`/`higher`/`lower` 계산·정렬은 수정하지 않고, 각 기존 그룹의 화면 전달값에 `universityGroups`만 추가했다.
- `groupResultsByUniversity(results)`는 이미 정렬된 평면 결과를 `universityId` 기준으로 묶고 `universityId`, `universityName`, `resultCount`, `results`를 반환하며 대학 내부 결과 순서를 그대로 보존한다.
- 학종은 기존 직접 비교용 `difference`를 만들지 않은 채 `referenceGrade`, `referenceDifference`, `referenceGroup`을 별도로 갖는 `comprehensiveReferenceGroups`를 추가했다.
- 학종 참고값은 공식 5등급 환산 70% cut, 공식 평균등급, 50% cut 순으로 존재하는 값을 사용하며, 값이 없는 레코드는 참고 구간에서 제외한다. 평균값을 cut으로 바꾸지는 않는다.
- 학생 내신 2.00 기준 학종 참고 구간도 1.80·2.20은 비슷한 범위, 1.79는 높은 쪽, 2.21은 낮은 쪽 경계로 검증했다.
- 교과·학종 대학별 그룹, 내부 순서 보존, 학종 참고 구간, 기존 교과 경계를 포함한 전체 회귀 테스트 129개가 통과했다.
- 사용자/Aside 수정 중인 `index.html`과 `styles.css`는 변경하지 않았다.

## 1학년 성취도 전용 과목 기본 입력 제외

- 학생용 `commonCourses()`와 `coursesForSemester()`는 기본적으로 `grade`/`both`이면서 5등급 산출 대상인 과목만 반환하도록 분리했다.
- 1학년 기본 입력에는 공통국어·공통수학·공통영어·한국사·통합사회·통합과학만 남고, 과학탐구실험·체육·음악·미술·보건·진로와 직업은 기본 화면에서 제외된다.
- 성취도/P·F 과목은 카탈로그에서 삭제하지 않고 `achievementCoursesForSemester()`와 `includeAchievementCourses` 옵션으로 선택 조회·표시할 수 있게 보존했다.
- 기존 학생 저장 데이터의 성취도 레코드는 `state.actual`에 그대로 유지하며, 기본 상태에서는 렌더링만 제외한다. 저장값의 성취도도 삭제하거나 초기화하지 않는다.
- 계산 모듈은 `achievement`, `passfail`, `fiveLevelEligible: false` 레코드에 숫자 등급이 남아 있어도 전체·학기·교과 평균, 반영 학점, 목표 내신 필요 평균에서 제외하도록 방어했다.
- 기본/선택 과목 분리, 기존 데이터 보존 경로, 성취도 과목 계산 제외를 포함한 전체 회귀 테스트 132개가 통과했다.
- `index.html`, `styles.css`는 수정하지 않았다. Aside의 기존 `app.mjs` 변경도 보존했다.

## 다중 학생 프로필 화면 연결

- 기존 `student-profile-store.mjs`의 학생별 프로필 저장·전환·초기화·마이그레이션 로직이 앱 상태에는 연결되어 있었지만, 학생 화면에는 프로필을 선택하거나 추가하는 조작 UI가 없음을 실제 로컬 브라우저에서 확인했다.
- 학생 정보 카드에 저장된 학생 선택, 새 학생 추가, 현재 학생 삭제, 모든 학생 삭제 기능을 연결했다. 저장 학생은 `학번 · 이름`으로 표시하며 5자리 학번을 프로필 키로 유지한다.
- 학생 전환과 새 학생 시작 전에 현재 입력을 저장하고, 선택한 학생의 성적·목표·관심 대학을 복원한다. 현재 학생 삭제는 다른 프로필을 보존하며 모든 학생 삭제도 학교·과목 공통 설정에는 영향을 주지 않는다.
- 로컬 `127.0.0.1:5500` 새로고침 후 학생 정보 카드에서 네 가지 프로필 조작 요소가 실제로 노출되고, 프로필이 없는 초기 상태에서는 삭제 버튼이 비활성화되는 것을 확인했다.
- `styles.css`는 변경하지 않고 기존 반응형 `student-form`과 버튼 클래스를 재사용했다. 전체 회귀 테스트 132개가 통과했다.

## 입시결과 그룹 아코디언 동작 복구

- 결과 그룹 `<details>`가 렌더링 때마다 하드코딩된 기본 `open` 상태를 사용하고 그룹 헤더용 상태·클릭 처리가 없어, 실제 공개 화면에서 헤더 클릭 후에도 `open` 값이 바뀌지 않는 문제를 재현했다.
- `createAdmissionAccordionState()`로 교과·학종 결과 그룹별 펼침 상태를 독립 관리하고, 동적으로 다시 그려지는 결과 영역에 이벤트 위임 방식의 그룹 헤더 클릭 처리를 연결했다. 클릭 시 상태, DOM `open`, `aria-expanded`가 함께 바뀌므로 기존 CSS 화살표 방향도 자동으로 갱신된다.
- 교과의 `similar`/`higher`/`lower`와 학종의 참고 구간·수치 미공개 그룹은 모두 최초 접힘으로 통일했다. 필터 또는 교과/학종 모드 변경 시 펼침 상태와 페이지 상태를 함께 초기화한다.
- 결과 그룹을 펼치면 대학은 최초 10곳만 렌더링하고 `대학 더 보기`로 10곳씩 추가한다. 대학 내부 모집단위는 기존 5개 단위를 유지한다.
- 로컬 브라우저에서 교과 세 그룹과 학종 세 그룹의 기본 접힘, 반복 토글, 여러 그룹 동시 펼침, 필터 초기화, 대학 10→20개 더 보기, 대학 내부 5개, 중첩 대학 아코디언과의 비충돌을 확인했다.
- `index.html`과 `styles.css`는 변경하지 않았다. 신규 상태 테스트를 포함한 전체 회귀 테스트 136개가 통과했다.

## 상단 백업 버튼 문구 정리

- Aside 변경분을 검토해 `JSON 내보내기`를 `내 데이터 백업`, `JSON 불러오기`를 `백업 불러오기`로 학생 친화적인 문구로 바꾼 것을 확인했다.
- `export-button`, `import-input`, 파일 형식과 기존 이벤트 연결은 그대로 유지되어 JSON 백업·복구 동작에는 영향이 없다.

## 학생 상담용 A4 요약 결과표

- 인쇄용 최신 상태 계산을 `buildPrintReportModel()`로, HTML 생성을 `renderPrintReport(model)`로 분리해 화면 상태와 출력 구조의 결합을 줄였다.
- 현재 선택 학생의 학번·이름·작성일만 출력하고, 전체 내신·입력 완료 학기·학기별 평균과 입력 상태·계산 기준·학점 정보 표현을 상담용 요약으로 정리했다. 미입력 학기는 0이 아니라 `미입력`으로 표시한다.
- 상세입력이 완료된 학기는 저장된 간편입력보다 우선하며, 간편·상세 혼합 시 잘못된 단일 반영 학점 대신 `상세 입력 학점 + 간편 입력 학기`를 구분해 표시한다.
- 목표 내신, 완료/남은 학기, 필요 평균, 도달이 어려운 경우 최고 도달 가능 내신과 균형형·초반 집중형·후반 상승형 학기별 시나리오를 추가했다. 각 시나리오는 학기별 한 줄로 집계한다.
- 전국 검색 결과 대신 현재 학생이 저장한 관심 대학만 출력하고, 학생부교과와 학생부종합을 별도 표로 분리했다. 교과는 9등급 원본·5등급 환산 50/70% cut과 최신 현재/목표 내신 기준 차이를, 학종은 평균등급·50%·70% 등 실제 공개 자료 유형과 원본/환산 참고값만 표시한다.
- 저장 당시 `comparisonScore`는 인쇄에 사용하지 않고 최신 계산 상태와 `comparisonBasis`로 다시 계산한다. 관심 대학 저장 모델은 평균등급과 자료 메타정보도 cut으로 변환하지 않고 보존하도록 확장했다.
- 과목별 상세 성적은 기본 본문에서 제거하고 `courseAppendix.included`가 켜질 때만 출력되는 선택 부록 구조로 분리했다. 성취도 전용 과목은 부록에서도 내신 미반영으로 구분한다.
- 일반 화면 `index.html`과 `styles.css`는 수정하지 않았다. Windows 샌드박스의 Node 테스트 파일 자동 분리 실행 제한으로 `npm test`는 `spawn EPERM`이 발생했으나, 각 테스트 파일을 동일 프로세스로 순차 실행해 전체 회귀 테스트와 신규 인쇄 테스트 6개가 모두 통과했다.

## 성취도 전용 과목 선택 추가 복구

- 성취도/P·F 과목을 자동 생성 대상, 학교 개설과목 선택 대상, 학생이 이미 추가한 입력 기록의 세 경로로 분리했다.
- 1학년 기본 자동 생성에는 공통국어·공통수학·공통영어·한국사·통합사회·통합과학 등 5등급 산출 과목만 유지한다.
- `selectableCoursesForSemester()`를 추가해 활성화된 학교 개설과목 전체를 선택 목록에 제공한다. 과학탐구실험·체육·음악·미술·보건·진로와 직업 등도 실제 학기와 반별 `classConditions`에 맞을 때 선택할 수 있다.
- 학생이 선택한 성취도 과목은 `gradingType: achievement`, `fiveLevelEligible: false`로 저장되고 등급 select 없이 A/B/C 성취도만 입력한다. P/F 과목은 기존 이수 여부 입력 방식을 유지한다.
- 기존 프로필에 저장된 성취도 과목은 전역 표시 플래그와 관계없이 다시 렌더링하며, 과목 ID가 없는 이전 저장 레코드도 gradingType·성취도·교과군을 보존하도록 호환 복원을 강화했다.
- 성취도/P·F 과목은 기존 계산 방어 규칙에 따라 전체·학기·교과 평균, 반영 학점, 목표 내신과 남은 학기 필요 평균에서 계속 제외된다.
- 로컬 `127.0.0.1:5500`에서 기본 등급 과목, 학교 개설과목의 성취도 선택지, 기존 과학탐구실험·체육의 A/B/C 입력을 확인했다. `index.html`과 `styles.css`는 수정하지 않았고 전체 회귀 테스트 143개가 통과했다.

## 입시결과 카드 세부 정보 조건부 렌더링

- 입시결과 카드의 세부정보 판별과 HTML 생성을 `admission-card-details.mjs`로 분리했다.
- 모집인원, 경쟁률, 50% cut, 평균등급, 추가 입결 설명, 별도 지원자격 또는 지역인재 요건 중 실제 값이 하나라도 있을 때만 `<details>`와 `세부 정보` summary를 생성한다.
- 모든 정규화 레코드에 공통으로 존재하는 기준학년도와 자료 출처만으로는 빈 아코디언을 만들지 않고, 실제 상세 항목이 있을 때 보조 메타정보로 함께 표시한다.
- 학생부교과와 학생부종합의 일반 카드 및 대학별 아코디언 내부 모집단위 카드가 동일한 조건부 렌더링 함수를 사용한다.
- 세부정보가 있는 경우 브라우저 기본 `<details>` 동작으로 최초 접힘과 펼침/접힘을 유지하며 기존 CSS 클래스와 디자인은 변경하지 않았다.
- 빈 상세정보 DOM 미생성, 단일 상세값 표시, 교과 50% cut, 학종 평균등급, 지원자격·지역인재 설명을 검증하는 테스트를 추가했고 전체 회귀 테스트 148개가 통과했다.

## 학생용 기능 최종 통합 점검(인쇄 제외)

- 학생 A/B 생성·자동저장·전환·삭제·관심 대학 분리와 기존 단일 저장값 마이그레이션을 통합 점검했다. 실제 브라우저에서 프로필 select의 `input` 이벤트가 학생정보 자동저장과 충돌해 선택이 즉시 되돌아가던 버그를 수정했다.
- 5개 대입 시뮬레이션 학기(1-1, 1-2, 2-1, 2-2, 3-1) 중 중간 미입력 학기가 있어도 남은 학기에서 누락되지 않도록 판정을 보완했다. 시나리오 3종은 브라우저에서 학기별 1줄·예상 최종 내신 1회 표시를 재확인했다.
- 성취도 전용 과학탐구실험을 학교 개설과목에서 추가해 A를 저장하고 다른 학생으로 전환한 뒤 다시 복원했다. 현재 내신·반영 학점·목표 시뮬레이션에는 계속 제외된다.
- 대학 선택지를 입결 레코드가 아닌 220개 대학 마스터 기준으로 생성하도록 재연결했다. 부산 14개교와 부산대·국립부경대·국립한국해양대 노출, 부산대 선택 시 인문·자연·예체능·기타/미분류 계열 선택지를 실제 브라우저에서 확인했다.
- 학교 공통 설정을 학생 프로필과 분리한 `naesin-school-settings:v1`로 정규화하고 부산 남고를 기본값으로 적용했다. 여자대학교 7개교는 대학 마스터에서 삭제하지 않고 남고 기본 검색에서만 제외한다. 지역인재는 저장된 공식 지원지역을 현재 학교 지역으로 다시 판정한다.
- 교과·학종 결과 그룹 전체 기본 접힘, 그룹 독립 토글, 대학 아코디언 `aria-expanded`, 부산 종속 필터, 학종 비교 금지 문구를 브라우저에서 확인했다.
- 현재 16,259건은 교과 10,136건·학종 6,123건이며 `sourceUrl`, `referenceYear`, `admissionCategory`, `universityId` 누락은 각각 0건이다. 원본·환산값 분리와 부산교육청 실측 보간 검증을 유지했다.
- JSON 백업 생성·형식 검증을 순수 함수로 분리해 잘못된 JSON과 타 형식을 안전하게 거부하도록 테스트했다. 복원은 대상 학생 프로필만 교체하고 다른 학생과 학교 공통 설정을 유지한다.
- 390·768·1024·1366px에서 학기 탭, 입력, select, 아코디언, 시나리오 카드가 노출되고 가로 overflow가 없음을 확인했다. UI 재설계와 `index.html`·`styles.css`·인쇄 코드는 변경하지 않았다.
- 신규 11개를 포함한 전체 회귀 테스트 159개가 모두 통과했고 실패·건너뜀기·취소는 0개였다.

## 로컬 학생 데이터 테스트 모드

- URL 쿼리의 `test=1`을 판별하는 학생 저장 런타임을 추가했다. 일반 접속과 GitHub Pages는 기존 `localStorage` 저장·복원 구조를 그대로 사용한다.
- 테스트 모드는 학생 프로필, 성적, 목표 내신, 관심 대학, 임시 입력과 JSON 복원 대상을 페이지 수명 메모리 저장소로 격리한다. 기존 `localStorage` 학생 데이터는 읽거나 삭제하지 않는다.
- 학교 공통 설정과 학교 과목 카탈로그는 학생 프로필과 분리된 기존 `localStorage`를 계속 사용한다.
- 로컬 `?test=1` 화면에 임시 학생을 입력한 뒤 새로고침해 학생 선택·학번·이름이 빈 상태로 돌아오는 것을 실제 브라우저에서 확인했다.
- 일반 모드 복원, 테스트 모드 빈 시작, 새로고침 초기화, 영구 저장소 무변경, 일반 URL 복귀 후 기존 학생 복원을 검증하는 신규 테스트 5개를 추가했다. 전체 회귀 테스트 164개가 모두 통과했다.
- `index.html`과 `styles.css`는 변경하지 않았다.
